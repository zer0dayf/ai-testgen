# Deps of the orchestrator itself (ai_testgen.py) — not of the repo under test
# (that one's deps come from the preset/config `install` command).
# Upper bounds block breaking MAJOR releases only; minor/patch updates still flow.
anthropic>=0.40,<2
openai>=1,<3
tomli>=2; python_version < "3.11"
